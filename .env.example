# =================================================================
# CODE REVIEW AI - Environment Configuration
# =================================================================
# Copy this file to .env and update with your actual values
# IMPORTANT: Never commit .env file to version control
# =================================================================

# -----------------------------------------------------------------
# Database Configuration - PostgreSQL
# -----------------------------------------------------------------
# PostgreSQL database name for the application
POSTGRES_DB=code_review

# PostgreSQL username (default: postgres)
POSTGRES_USER=postgres

# PostgreSQL password - CHANGE THIS IN PRODUCTION!
POSTGRES_PASSWORD=your_secure_postgres_password_here

# PostgreSQL port (default: 5432)
POSTGRES_PORT=5432

# Full database connection URL for SQLAlchemy
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:your_secure_postgres_password_here@localhost:5432/code_review

# -----------------------------------------------------------------
# JWT Authentication
# -----------------------------------------------------------------
# Secret key for JWT token signing - MUST BE RANDOM AND SECURE!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your_random_jwt_secret_key_here_change_this

# JWT algorithm (recommended: HS256)
JWT_ALGORITHM=HS256

# JWT token expiration time in hours
JWT_EXPIRATION_HOURS=24

# -----------------------------------------------------------------
# Encryption
# -----------------------------------------------------------------
# Fernet encryption key for GitLab tokens - MUST BE SECURE!
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_fernet_encryption_key_here_change_this

# -----------------------------------------------------------------
# LLM Configuration - Ollama
# -----------------------------------------------------------------
# Ollama server base URL
OLLAMA_BASE_URL=http://localhost:11434

# Ollama model name (e.g., llama2, codellama, qwen2.5-coder)
OLLAMA_MODEL=qwen2.5-coder:latest

# -----------------------------------------------------------------
# Redis - Caching Layer
# -----------------------------------------------------------------
# Redis server port
REDIS_PORT=6379

# Redis password - CHANGE THIS IN PRODUCTION!
REDIS_PASSWORD=your_secure_redis_password_here

# Redis connection URL (optional - for advanced configuration)
# REDIS_URL=redis://:your_secure_redis_password_here@localhost:6379/0

# -----------------------------------------------------------------
# Qdrant - Vector Database
# -----------------------------------------------------------------
# Qdrant server port
QDRANT_PORT=6333

# Qdrant API key (optional - for authentication)
# QDRANT_API_KEY=your_qdrant_api_key_here

# -----------------------------------------------------------------
# Backend Server Configuration
# -----------------------------------------------------------------
# Backend server port
BACKEND_PORT=3000

# Backend server host (0.0.0.0 allows external connections)
BACKEND_HOST=0.0.0.0

# CORS allowed origins (comma-separated)
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# -----------------------------------------------------------------
# Frontend Configuration
# -----------------------------------------------------------------
# Backend API URL for frontend requests
VITE_API_URL=http://localhost:3000

# -----------------------------------------------------------------
# Environment
# -----------------------------------------------------------------
# Application environment (development, staging, production)
NODE_ENV=development

# -----------------------------------------------------------------
# Optional: Production Settings
# -----------------------------------------------------------------
# Uncomment and configure for production deployment

# Enable debug mode (true/false)
# DEBUG=false

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Session secret key for additional security
# SESSION_SECRET=your_session_secret_here

# SSL/TLS configuration
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem

# -----------------------------------------------------------------
# Optional: Email Configuration (for future features)
# -----------------------------------------------------------------
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your_email@gmail.com
# SMTP_PASSWORD=your_email_password
# SMTP_FROM_EMAIL=noreply@yourdomain.com

# -----------------------------------------------------------------
# Optional: Monitoring & Analytics
# -----------------------------------------------------------------
# SENTRY_DSN=your_sentry_dsn_here
# GOOGLE_ANALYTICS_ID=your_ga_id_here

# =================================================================
# End of Configuration
# =================================================================
